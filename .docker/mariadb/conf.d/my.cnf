[mysqld]

# 設定 InnoDB 儲存引擎的 redo log 檔案大小為 4048 MB（大約 4 GB）。
# 這個數值愈大，對於大量寫入的資料庫效能愈好，但重啟時 crash recovery 會變慢。
# 適合高寫入量或大型資料庫
innodb_log_file_size = 4048M

# 關閉 InnoDB 的嚴格模式。
# 嚴格模式下，某些錯誤（如欄位長度不符）會直接報錯並阻止寫入。
# 設為 0 代表較寬鬆，部分錯誤會給警告但仍允許寫入。
innodb_strict_mode = 0